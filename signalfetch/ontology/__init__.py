"""Semantic layer: RDF/OWL ontology as a macro/funds extension to FIBO.

This module maps DataPoints onto RDF/OWL concepts, enabling SPARQL queries,
semantic reasoning, and integration with the Financial Industry Business Ontology
(FIBO). Requires the `ontology` optional dependency group (rdflib).
"""
