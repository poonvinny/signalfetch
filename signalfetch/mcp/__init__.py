"""MCP server exposing SignalFetch agents as tools.

This module wraps registered agents in an MCP (Model Context Protocol) server,
allowing LLM assistants to call agents as structured tools. Requires the `mcp`
optional dependency group.
"""
